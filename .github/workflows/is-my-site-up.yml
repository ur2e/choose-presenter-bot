name: Workflow for testing ping action
on: workflow_dispatch

jobs:
  build:
    name: ping
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./
        with:
          site: "https://6556-218-155-31-211.ngrok-free.app"
